const active = true;

// let message = "";

// if (!active) {
// 	message = "Active";
// } else {
// 	message = "Disabled";
// }

// let message = active ? "Active" : "Disabled";
// let message = active ? "Active" : null;

const message = active && "Active";

console.log(message);
